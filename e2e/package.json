{
  "name": "calendar-booking-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright e2e tests for the calendar booking app (frontend + backend).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^26.1.2"
  }
}
