# Base URL of the backend API. Leave empty (or omit the file) to use
# same-origin requests; in dev the Vite server proxies /event-types and
# /bookings to http://localhost:3000 (see vite.config.ts).
VITE_API_URL=
